package com.codingproblems.uber;

public class ProductCalculator {

  /**
   * Returns an int array containing at each index the product of all other indexes in {@param arr}
   * At index i the value will be arr[0] * arr[1] * ... * arr[i-1] * arr[i+1] * ... * arr[arr.length]
   */
  public static int[] calculateProductOfOthers(int[] arr) {
    //TODO implement
  }
}
